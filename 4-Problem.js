// 4-Problem.js statement - Create two variables for **first and last name**, concatenate them, and log the result.

const firstName = "Anirudha";

const lastName = "Bele";

console.log(`Full name is: ${firstName} ${lastName}`);
